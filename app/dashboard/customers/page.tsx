import { Metadata } from 'next';
 
export const metadata: Metadata = {
  title: 'Invoices',
};

// ... kode komponen Invoices kamu tetap di bawahnya
export default function Page() {
  return <p>Customers Page</p>;
}
